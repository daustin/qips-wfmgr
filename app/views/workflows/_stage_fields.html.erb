<div class="fields">
<%= f.select :protocol_id,  Protocol.all.collect {|pt| ["#{pt.name}", pt.id]}, :include_blank => true %>   
<%= f.hidden_field :_destroy %>   
<%= link_to_remove_fields "remove", f %>
</div>

