<p>	
<% case parameter.field_type  when 'text' %>

<%= parameter.pretty_name %>: <%= p.text_field parameter.var_name, :value => parameter.initial_value %> 

<% when 'check_box' %>

<%= parameter.pretty_name %>: <%= p.check_box parameter.var_name %>

<% when 'radio_box' 
# enumerate through choices
%>
<%= parameter.pretty_name %>: <BR>
<% parameter.value_options.split(',').each do |v| 
	
	checked = v.strip.eql?(parameter.initial_value.strip)
	
	-%>
<%= p.radio_button parameter.var_name, v, :checked => checked %> <%= v.strip %>
<% end -%>
<% when 'select' %>
<%= parameter.pretty_name %>: <%= select_tag parameter.var_name, options_for_select(parameter.value_options.split(','), parameter.initial_value) %>
<% else %>

	
<% end%>
</p>	
 